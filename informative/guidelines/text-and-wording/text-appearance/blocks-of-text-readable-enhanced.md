## Tests

<b>Procedure</b>

For each block of text:
1. <b>HTML:</b> Check CSS to confirm that relevant style attributes (such as line height, letter spacing, word spacing, text align) are set within the values specified in the provision.
2. <b>Non-web apps:</b> Check GUI toolkit settings (or defaults) to confirm that they apply values within the designated range.

<b>Expected results</b>
* #1 or #2 is true.
